{
  "name": "claude-function-hooks",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'plugins/*/src/**/*.test.ts'",
    "typecheck": "npx -y -p typescript@5 tsc -p .",
    "format": "npx -y prettier@3 --write 'plugins/**/*.{ts,tsx,json}'",
    "validate": "for p in plugins/*/; do claude plugin validate $p || exit 1; done"
  }
}
